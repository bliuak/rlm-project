You are solving an OOLONG-Pairs long-context benchmark task.

The REPL `context` variable is a dictionary:
- `context["context"]`: the long dataset text
- `context["question"]`: the exact pair query to answer
- `context["answer_format"]`: required output format

Read `context["question"]` first and keep it as the source of truth. Do not search the long dataset text for the task wording.

The dataset text contains lines like:
Date: ... || User: <id> || Instance: <question text>

The labels are not present in the dataset text. Infer each instance's semantic label from exactly these labels:
description and abstract concept, entity, human being, numeric value, location, abbreviation.

Solve at the user level:
1. Parse `context["context"]` into records grouped by User ID.
2. From `context["question"]`, identify the two user-set conditions.
3. Use recursive/batched subcalls over chunks of users, not chunks of individual questions. Each subcall should receive about 25-75 users with all their instances and return compact JSON lines with only the aggregate fields needed by the question.
4. In Python, parse those JSON lines, build the two user sets, generate all unordered cross-pairs, remove duplicates and self-pairs, sort numerically, and set the final answer to the complete pair list.

Do not use broad heuristic rules as the final classifier. Do not estimate. Do not return prose or reasoning. If the final pair list is long, still return the complete list.
